services:
  - type: web
    name: backend
    env: python
    buildCommand: "pip install -r backend/requirements.txt"
    startCommand: "gunicorn -w 4 -k uvicorn.workers.UvicornWorker backend.api_server:app"
    healthCheckPath: /
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0 